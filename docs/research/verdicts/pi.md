# pi arena verdict — workflow wf_16f36a94-0ca, 2026-09-12

pi commit 71dca871. 27 agents, 0 errors. Ranking C > B > A; base C; no must-pass failures.

## Judge 1
Ranking ['C', 'B', 'A'] base C

### Fold-ins
- **from B**: The pi manifest short-circuits convention directories: when `package.json` carries a `pi` block, pi loads ONLY what the manifest lists and never walks `skills/`, `prompts/`, `themes/` (package-manager.ts:2174-2187, `return true` after `addManifestEntries`). SkillSpector's manifest lists `extensions` only (ss:package.json:7-9), so its own `skills/skill-inspector/SKILL.md` — the APPROVE/CAUTION/REJECT skill that is the product's primary shape — never loads inside pi. I verified both halves directly. This is the sharpest single actionable finding in either runner-up because skillgate walks straight into the same trap: recommendation.md:11 makes skillgate 'one Go binary plus a `skill-gate` Agent Skill wrapping it — shaped like NVIDIA's skill-inspector', so a pi package that lists only `extensions` ships the binary and silently drops the skill that is supposed to be the interface.
  - where: Into C's §3 as a new 'Cannot / silently broken' row beside the dropped-`env` defect, and as a hard requirement on C's rec #1 (publish `skillgate-pi`): the manifest must list `skills` alongside `extensions`, with a test that asserts the skill is discoverable after `pi install`. Also worth a line in C's §8 P1, since it is evidence that the pi-package path needs its own acceptance check rather than being assumed cheap.
- **from A**: 'Gate ordering, not privilege, decides.' `tool_call` handlers run in extension load order, later handlers observe earlier mutations of `event.input`, the first `{block:true}` short-circuits, and any extension may re-register `read`/`bash` (runner.ts:982-1003; types.ts:939-944; examples/extensions/tool-override.ts). Therefore a skillgate gate extension can *audit before load* but can never *contain* an extension already loaded — and A is the only candidate that converts this from a fact into a constraint on what the product may claim, and then extends the same candor to Claude Code hooks and Cursor `beforeReadFile`.
  - where: Into C's §7 (replacing the weaker 'containment is OS/container level' row), into the wording of C's rec #4 and decision P2, and upstream into recommendation.md's refusal set beside F12/F13 — which today bounds what the *ledger* may claim but says nothing about what a *runtime gate* may claim. It is the one fold-in that changes what the product is allowed to say, which is exactly the F12/F13 class.

### Defects
- ACTIVATION IS NOT DETERMINISTIC ON pi (A §4 'Skill-activation signal ● deterministic'; C insight 6 and rec #7). pi's own docs say the agent uses '`read`, or `bash` when `read` is unavailable, to load the full SKILL.md (models don't always do this...)' (docs/skills.md:69), and `formatSkillsForPrompt(skills, fileReadTool)` emits the bash phrasing when read is absent (skills.ts:355; system-prompt.ts:160). Nothing stops a `bash cat` when read IS present, and `read` takes `offset`/`limit` (tools/read.ts:14-17) so partial reads are unclassified. B's 'partial — deterministic but reconstructed' is the correct calibration. Load-bearing: recommendation.md threat #14 measures activation precision and recall, and rec:102 already flags Cursor's U-02 heuristic as untested — promoting pi to ground truth would corrupt the slice-4 comparator. Invariant the merge must hold: pi activation is observable only when the model uses `read`, with a named bash-fallback and partial-read blind spot.
- THE CACHE-MISS DETECTOR DOES NOT PRICE A SKILL LOAD — shared root across all three. `detectMiss` computes `missedTokens = min(prev.promptTokens, promptTokens) − cacheRead` and discards anything ≤ `NOISE_FLOOR_TOKENS = 1024` (cache-stats.ts:11,70). That measures prefix *invalidation*. Loading a SKILL.md *appends* to a cached prefix, so `cacheRead ≈ prev.promptTokens` and `missedTokens ≈ 0` — the detector returns `undefined` and the skill load is invisible to it. It also runs per assistant message with no skill attribution (cache-stats.ts:120-128), and is not exported from the package entry, so 'borrow' means copying the file, not importing it. Severity by candidate: B worst (insight 7 + rec 7 both build on it), C next (rec 8 imports the miss-detector's dollar formula and its 1,024 floor onto a cacheWrite delta), A least (frames it as the raw `cacheWrite`/`cacheRead` delta and tags it [I]). Second collision: the detector outputs dollars and recommendation.md:110 says skillgate 'Never prices anything — minutes and tokens only.' Fix direction: keep the raw per-message `cacheWrite` delta as a measured-token signal under rec §5 tier 2; drop the miss detector and its pricing.
- A's 'Claude Code assets are already live inside pi sessions' (A §1.5, insight 9, rec #1 rationale) is false as stated. Only `CLAUDE.md` is a default context candidate (resource-loader.ts:72). Ingesting `~/.claude/skills` requires a user-added settings entry — docs/skills.md:44-63 reads 'To use skills from Claude Code or OpenAI Codex, add their directories to settings' — and `examples/extensions/claude-rules.ts` is an example the user must copy. The premise still supports adding pi roots once restated honestly: 'CLAUDE.md is live by default; CC skills are one settings line away.'
- ALL THREE assert pi never passes `--ignore-scripts`. It does — for pi's own managed self-install (`npm ci --ignore-scripts --min-release-age=0 --omit=dev ...` at package-manager-cli.ts:89-92, and config.ts:141,153,163,179). The claim is true only of the *package* install path (package-manager.ts:1849-1857 git clone → `npm install --omit=dev`). The correct and sharper statement is B's §7 contrast: pi hardens its own supply chain (pins, shrinkwrap, `--ignore-scripts`, audit) and does not extend any of it to the packages it installs for you.
- MANIFEST FIELDS ARE SHAPE-VALIDATED, contra A's §2 'nothing else parsed or validated' / 'unvalidated' and C's 'validates nothing else'. pi-manifest.ts:23-29 keeps a field only when `Array.isArray(entries) && entries.every(e => typeof e === 'string')`; only the *contents* (paths) go unchecked. B states this correctly. Matters because A's rec #6 and C's rec #5 both argue G0 should treat the manifest as an executable surface — the argument survives, but the 'unvalidated' framing is wrong and will be challenged.
- CITATION DRIFT IN A on two load-bearing rows: `main.ts:111-122` is `resolveAppMode`, not the flag→`createAgentSession()` translation (that rests on the file's 1-6 header comment); and `types.ts:1125-1134` is `ToolCallEventResult` — `ProjectTrustHandler` is at `types.ts:540-543`. Both cites sit in A's §2 Composition and Gate-semantics rows.
- EVENT COUNT: A's '36 `on()` overloads' is exact (I counted 36 over types.ts:1250-1320). B and C both say '~40'. Trivial, but the merge should take A's number since it is the one that was actually counted.
- PACK E'S GAP IS NARROWER THAN B (#3) AND C (#2) STATE. recommendation.md:156 already enumerates 'hook definitions, MCP manifests, plugin metadata' as always-on items. The genuine gap is that a manifest file's bytes are not the same object as the tool-description text a server or plugin injects into the prompt each turn. Both must be restated as 'count the injected text, not the manifest file', or the merged doc reads as though it is adding an item class the contract already has — and the reviewer who checks rec:156 will discount the whole insight.
- DECLARED-BUT-UNCLOSED COVERAGE HOLES in all three. A's §8 closes with 'Claude Code and Cursor cells marked — were not verified from primary sources in this pass' (and two more unknowns); B's §7 carries a row explicitly marked 'not re-read under this lens'; C's §7 ends with an '[I] ... not re-fetched' row. The CC/Cursor facts all exist in the repo already (docs/research/cursor-telemetry-surfaces.md, skill-profiling-state-of-the-art.md, skill-audit-tool/L2-dynamic-report.md). The merge should either resolve them from those documents or delete the rows; shipping a candor section with its own hole is self-defeating.
- LENGTH — all three miss B6's five-page bar, B worst. Row counts across the seven substantive sections: B ≈ 94, A ≈ 83, C ≈ 79, and B front-loads 35 rows of pi internals (§2 + §3) before the overlap map. The cut that costs nothing is the 'What pi is' encyclopedia: keep only rows that carry a skillgate column (skill roots, always-on assembly, trust, `tool_call`, packages/install, sessions) and drop monorepo build order, CLI composition, published devDependencies, the 36-event inventory, and the dead telemetry plumbing — the last of which belongs solely in the one-line 'do not build on this' avoid row all three already have.
- TWO RECOMMENDATIONS CARRY MEDIUM EFFORT AGAINST SELF-DECLARED ZERO VALUE FOR BOTH TARGETS: B #10 (`skillgate doctor` pi checklist, 'none' / 'none', M) and A #7 (`skillgate doctor --harness pi`, Low / Low, M). These are the rows most exposed to B2's 'not generic pi commentary'. Either demote to deferred or restate the value for the CC/Cursor user who also runs pi — which is the only framing that earns the row.
- EFFORT FOR THE pi PACKAGE DISAGREES 2:1 — C says S (citing SkillSpector's ~140-line extension as precedent, which I confirmed at ss:extensions/skillspector.ts:83-141), A and B say M. C's S is defensible for the shell itself but does not price the `tsc`-against-pinned-host-types CI check and the lockstep re-test that all three separately demand given pi's explicit no-backward-compatibility policy (AGENTS.md:25). The merge must pick one number and say which cost it includes.
- JUDGMENT CALL RECORDED, NOT SCORED AS A FAILURE: B3 says recommendations must name Claude Code value and Cursor value separately with effort. A leaves all three cells em-dashed on its 'avoid' row (#12); B leaves them em-dashed on two ('avoid' rows #11 and #12). C fills all twelve rows including its avoid row ('Prevents a dead surface in slice 4' / 'Same' / 'S'), proving it can be done. I did not fail A or B on this — an em-dash on an avoid row is a defensible reading of 'named separately', and failing on it would misdirect the merge — but the merged document should follow C and fill every cell.
- VERIFIER DISAGREEMENT THE MERGE SHOULD NOT INHERIT BLINDLY: B's relevance verifier returned 0/15 and justified it partly with 'pi is not a target harness, which candidate B never argues.' That is wrong about B's document — B argues exactly that in its §1 line 3, its §4 gate-stage audit, and its §8 decision 1 ('Make pi a third target harness'). The extraction evidently sampled B's §2/§3 (pi facts) rather than its §4/§5, where every cell is anchored to a `rec:` line. B's real B2 exposure is the 35-row pi-internals block, not an absence of skillgate framing. I ranked B on my own reading of the document, not on that verdict.

### Scorecard
## Coverage

**3/3 candidates × 8/8 sections walked (24 section-units) · 6/6 bar criteria × 3 candidates (18 cells) · 28 independent spot-checks** against `scratchpad/pi` @71dca87, `scratchpad/skillspector-judge` @8421a2e, and the live npm registry (target was ≥10) · `docs/research/recommendation.md` (249 lines) and `.scuba/teams/pi/mandate.md` read as the relevance baseline. Second sweep over all three documents added no new finding.

## Ranking

**C > B > A.** Base: **C**.

| | B1 traceable / not source-refuted | B2 about skillgate for CC+Cursor | B3 CC value + Cursor value + effort | B4 insight | B5 candor | B6 readability ≤5pp |
|---|---|---|---|---|---|---|
| **A** architecture-first | **PASS** (15/15 source-confirmed; but C11 feasibility-refuted and load-bearing for rec #1) | **PASS** — §4/§6 carry CC+Cursor columns; weakened by a 15-row pi encyclopedia in §2 and 7/15 sampled claims judged pi trivia | **PASS** — 11/12 rows filled; avoid row em-dashed | **4** | **4** | **3** |
| **B** overlap-first | **PASS** (15/15 source-confirmed; C14 feasibility-refuted and load-bearing for insight 7 + rec 7) | **PASS** — best spine of the three: every CC/Cursor cell `rec:`-anchored plus a G0–G7 gate-stage walk; weakened by 35 rows of pi internals before the map | **PASS** — 10/12 rows filled; two avoid rows em-dashed | **4** | **4** | **2** |
| **C** opportunity-first | **PASS** (15/15 source-confirmed, 15/15 feasibility-confirmed — the only candidate with zero refutations in either) | **PASS** — recs tie to *named* open items in the approach (#320, SP1, Pack D's "no primary source", tier 2, "never prices anything") | **PASS** — 12/12 rows filled including the avoid row | **5** | **4** | **4** |

**No must-pass failures.** B1 is written as "not refuted by the *source-accuracy* verifier," and that verifier refuted nothing for any candidate. A's "already live" and B's cache-miss overclaim were refuted by the *feasibility* verifier and independently by me; both are load-bearing, both are recorded as defects the merge must fix, but neither trips the criterion as written. I did not manufacture a failure the bar does not state.

## Why C is the base — soundest shape, not completeness

C's frame is the four decisions the mandate actually poses (borrow / third harness / ship as a pi package / dynamic attribution), so every section terminates in something the user can rule on. It carries the fewest things that must be torn out (zero source or feasibility refutations, highest relevance ratio at 10/15), it is the tightest of the three, and it is the only candidate that reconciles its borrows against the approach's own constraints — naming rec §5 tier 2 for measured tokens and acknowledging rec:110's "never prices anything." It is also the only candidate that *quantifies* the target decision: 9,653 npm packages carrying the `pi-package` keyword and ~2M weekly CLI downloads. I verified both live (registry search `total: 9654` today; `api.npmjs.org/downloads/point/last-week/@earendil-works/pi-coding-agent` = 1,981,039). A and B give no adoption number at all, which is precisely the number that decides "is pi worth targeting."

**B is second on spine, not on facts.** Its §4 overlap map with `rec:`-anchored cells plus the G0–G7 gate-stage audit is the most directly reusable structure of the three, and it holds two findings neither rival has. But it is the longest document, its headline borrow is mechanically wrong, and it collides with the approach's no-pricing rule.

**A is third despite having the best facts** — 36 `on()` overloads counted exactly, the `<available_skills>` read/bash gating, SkillSpector's CC-only analyzer paths. Architecture-first is the wrong frame for this bar: the reader traverses a pi encyclopedia before reaching anything that touches skillgate, the most-promoted recommendation rests on a refuted premise, two load-bearing cites drift, and the overlap map ships "—" cells the document itself admits were never verified. B's defect is excisable in one row; A's needs half the document re-framed.

## Spot-checks I ran myself (28; all `file:line` verified in-clone unless noted)

**Confirmed for all three:** `ExecOptions` = `{signal,timeout,cwd}` and `spawn` never forwards `env` (exec.ts:11-18,41-45) · `MAX_NAME_LENGTH 64` / `MAX_DESCRIPTION_LENGTH 1024` (skills.ts:11-14) · `formatSkillsForPrompt` emits the full description untruncated (skills.ts:355-383) · context-file candidate order and the ancestor walk to the filesystem root (resource-loader.ts:73-74,119-157) · `allowed-tools` has zero hits in `coding-agent/src` + `agent/src`, one docs line (skills.md:149) · local install is `existsSync` only (package-manager.ts:1018-1023) · git install = clone + checkout + `npm install --omit=dev` (:1849-1857, :1772-1778) · `emitToolCall` first-`block`-wins in extension order (runner.ts:982-1002) · `beforeToolCall` rethrows (agent-session.ts:483-501) · cost buckets are `provider/model` + `Tools/summaries` only (usage-totals.ts:41-52) · `TOOL_RESULT_MAX_CHARS = 2000` (compaction/utils.ts:88) · trust resource list (trust-manager.ts:30-38) · `skillspector-pi` → HTTP 404, `private: true`, `pi.extensions` only (live + ss:package.json).

**Checks that discriminated between candidates:** `docs/skills.md:69` bash fallback + "models don't always do this" → refutes A's and C's deterministic-activation claim, vindicates B's "partial" · `docs/skills.md:44-63` "add their directories to settings" → refutes A's "already live" · `package-manager.ts:2174-2187` manifest short-circuits convention dirs → confirms B's unique invisible-skill finding · `package-manager-cli.ts:89-92` + `config.ts:141,153,163,179` use `--ignore-scripts` → all three's blanket claim needs scoping, B's §7 contrast is the correct form · `pi-manifest.ts:23-29` validates `string[]` → A's "unvalidated" overstates, B correct · `cache-stats.ts:11,70,120-128` + `tools/read.ts:14-17` + no export from `index.ts` → the miss detector cannot see an appended skill body · `types.ts` 36 `on()` overloads counted, `ReadToolCallEvent` at 905-908, `ToolCallEventResult` at 1125-1134, `ProjectTrustHandler` at 540-543 → A's count exact, A's two cites drifted · registry `latest 0.85.1`, 45 versions → C exact · `AGENTS.md:97-99,118-120` "load and follow `.pi/skills/…`" → C's ICM-index borrow accurate · `check-entry-graphs.mjs:1-11` "Entry points are cost contracts", `test.sh:39-52` `env -i`, `permission-gate.ts:20-23` block-when-`!hasUI`, `extensions.md:2934` "`tool_call` errors block the tool (fail-safe)" → A's and C's borrow claims accurate · `bundled_execution_surface.py:38-44` `_APPLICABLE_PATHS` is CC-only → A's harness-reach row accurate · no test in `ss:tests` names the extension → B and C accurate.

## Merge instruction

Build on **C**. Take **B's manifest short-circuit** into C's §3 and as a hard requirement on C's rec #1. Take **A's "gate ordering, not privilege"** into C's §7, the wording of C's rec #4/P2, and upstream beside F12/F13 in `recommendation.md`. Then clear the thirteen defects — the two that change conclusions rather than wording are the non-deterministic activation caveat and the removal of the cache-miss borrow.

## Judge 2
Ranking ['C', 'B', 'A'] base C

### Fold-ins
- **from B**: The gate-stage delta: an explicit G0 -> G7 block naming what each stage of our existing gate must add (G0 pi package sources npm:/git:@ref/local plus pi.extensions as executables; G1 pi diagnostics as ledger reasons and ignore-pruned-skill as a terminal 'silently skipped' outcome; G2 a T017-analogue for pi.extensions and T020 writes into .pi/, ~/.pi/agent/trust.json, settings.json#packages; G3 unchanged; G4 our thin delta owns .pi/settings.json since agnix has no pi rules; G5 Pack E gains tool guidelines and Pack G the pi silent-failure rules; G6 unchanged; G7 the pin also records the pi version the extension API was tested against). This is the single artifact that converts pi facts into edits to named stages of our gate rather than into per-pack advice, and it is what B2 is actually asking for.
  - where: Into the base's section 4, immediately after the overlap table, as a short 'what each gate stage must add' block. The base currently distributes this across section 6 recommendations 2, 5, 6 and 11, so it never shows the gate as a whole; folding it in also lets several of those recommendation rows shrink to one line each, buying back page budget.
- **from A**: pi as the ground-truth validation bench for Pack E. BuildSystemPromptOptions{contextFiles, skills, toolSnippets, promptGuidelines, customPrompt, appendSystemPrompt} (system-prompt.ts:8-25) is exactly and only what enters the prompt, and before_agent_start hands both systemPromptOptions and the assembled systemPrompt string to an extension (types.ts:717-727). So Pack E's per-item arithmetic can be checked item-by-item against the real prompt string on a live session -- a ground truth neither Claude Code nor Cursor exposes. That upgrades pi from 'a third target we might add' to 'the test fixture that proves the metric we are shipping for CC and Cursor is arithmetically right'.
  - where: Into the base's section 5 as the stated reason behind its recommendation 9 (in-harness ledger via before_agent_start + appendEntry), and as a named acceptance test for Pack E in slice 2 of the roadmap. The base already has the mechanism (rec 9) but sells it as a Cursor-hook-spool prototype; A supplies the reason that makes it worth doing for CC and Cursor users rather than pi users.

### Defects
- SHARED (A, B, C) -- REAL, reproduced. The cache-miss detector is mis-borrowed as a per-skill / per-activation cost signal (A insight 8 + rec 8; B insight 7 + rec 7; C insight 7 + rec 8). Verified at pi:packages/coding-agent/src/core/cache-stats.ts:56-71,110-128: missedTokens = min(prev.promptTokens, promptTokens) - usage.cacheRead, above a 1024-token floor. Loading a SKILL.md body APPENDS to context and leaves the cached prefix intact, so cacheRead ~= prev.promptTokens and missedTokens ~= 0. The detector measures prefix-cache waste from idle gaps, model switches and compaction resets (compaction and branch_summary entries explicitly reset prev at :113-118), not the cost of loading a skill. It also emits dollars, which collides with recommendation.md:110 'Never prices anything -- minutes and tokens only'. Invariant that must hold: a per-skill cost number is derived only from a quantity that changes when and only when a skill body enters context. Direction (advisory): the turn-over-turn prompt-token delta (usage.input + usage.cacheWrite on the turn following the SKILL.md read) is the candidate signal; the miss detector is not.
- A and C -- REAL. 'Activation in pi is deterministic' is overclaimed (A section 4 'Skill-activation signal: pi has, deterministic'; C insight 6 and overlap row 'deterministic'). Verified at pi:packages/coding-agent/docs/skills.md:69 -- the agent uses 'read, or bash when read is unavailable, to load the full SKILL.md (models don't always do this...)' -- and at pi:packages/coding-agent/src/core/system-prompt.ts:45-46, where skillFileReadTool selects bash whenever read is not in the tool set. Partial reads (offset/limit) and reads of referenced files are also unclassified. B's cell, 'partial -- deterministic but reconstructed', is the honest one; carry B's wording. This matters because the recommendation's threat row 14 is activation precision AND recall (recommendation.md:38).
- SHARED (A, B, C) -- REAL. The proposed pre-activation gate does not close the bash leg. A rec 5 gates 'unapproved SKILL.md reads'; B rec 6 is a 'tool_call read-gate by hash pin'; C rec 4 names read/bash but no candidate says how a shell command line is matched to a skill path. Since pi itself instructs the model to bash the SKILL.md when read is absent (system-prompt.ts:45-46), a read-only gate is bypassable by construction. Invariant: no un-pinned SKILL.md body may enter context by any tool. The merge must either cover the bash leg explicitly or scope the claim to 'blocks the read path; the bash path is unsolved' -- silently claiming a gate that leaks would violate F12/F13 (recommendation.md:190-191).
- A -- REAL. 'The estate skillgate audits for CC is already live inside pi sessions' (A insight 9, and the premise under A rec 1) is false for skills. Verified at pi:packages/coding-agent/docs/skills.md:44-63: using ~/.claude/skills or ~/.codex/skills requires the user to ADD those directories to a settings 'skills' array; a stock pi install loads no Claude Code skills. Only CLAUDE.md is live by default, as the fourth candidate in resource-loader.ts:72. B and C frame it correctly as opt-in configuration. Do not carry A's wording -- it inflates the size of the coverage gap that justifies adding pi roots.
- SHARED (A, B, C) -- REAL. All three blow the five-page bar by roughly 2x (A ~82 table rows, B ~90 rows plus prose, C ~69 rows plus 10 prose insights). The two largest non-load-bearing blocks are identical in all three: section 2 'What pi is' (14-18 rows of pi internals) and section 3 'How SkillSpector uses pi' (12-14 rows). Section 3 is the weakest by the bar's own test -- skillgate consumes the SkillSpector CLI under --no-llm (recommendation.md:58,111) and never the pi extension, so the wrapper's internals are off our integration path entirely. Direction: cut section 2 to only the rows the overlap table actually cites, and reduce section 3 to the one transferable lesson (thin TS shell over the real engine) plus the four defects not to repeat.
- SHARED (A, C) -- REAL. The npm 404 for skillspector-pi is presented as a distribution gap without the caveat that makes it meaningless: ss:package.json:4 sets "private": true, so the 404 is by design, not an oversight. A's section 3 'Distribution' row and C's rec-1 evidence both lean on it. Keep the fact, drop the implied criticism; the real distribution point is that a skillgate pi package WOULD need publishing, not that NVIDIA failed to publish.
- A -- REAL. Six of 34 Claude Code / Cursor cells in A's overlap table are em-dashes, and A's own closing paragraph concedes 'Claude Code and Cursor cells marked -- were not verified from primary sources in this pass'. Rows affected: Cross-harness ingestion (CC), Project trust (CC, Cursor), Package provenance (CC, Cursor), Compaction retention (Cursor). Since the two harness columns are exactly what B2 demands, blank cells there are the costliest possible omission. Note the inverse strength worth preserving: where A DID fill those cells it sourced them from sibling research primaries (skill-profiling-state-of-the-art.md:116,133-134; cursor-telemetry-surfaces.md:51-58,113; L2-dynamic-report.md), which I spot-checked and which resolve -- better provenance than B's and C's 'taken from rec: as written'. The merge should fill every cell AND source it from the primaries.
- B -- REAL. B's section 7 candor table contains a row that admits it was not verified: 'Per the verified map, pi-telemetry/harness spans are not emitted in production and the server stack is experimental; not re-read under this lens [I]'. An unverified row inside the candor section is the one place that cannot carry an [I]. I verified it for the merge: startAiSpan and startHarnessSpan have callers only in packages/agent/test/harness/telemetry.test.ts and their own definitions plus the index re-export; no package.json in packages/ declares any opentelemetry dependency. Carry it as observed, not inferred.
- B -- REAL, unique and must survive the merge (neither A nor C has it). SkillSpector's own Agent Skill never loads in pi. Verified: ss:package.json:7-9 declares a pi manifest listing only 'extensions'; pi:packages/coding-agent/src/core/package-manager.ts:2174-2187 returns immediately after processing a manifest, so the convention skills/ directory is never walked; ss:skills/skill-inspector/SKILL.md therefore never reaches pi's loader. This is the concrete, evidenced reason a skillgate pi package must list 'skills' in its manifest, and it is the sharpest single item in B's section 3.
- SHARED (B, C) -- SUSPECTED framing defect. The 'tool promptGuidelines / promptSnippets are an uncounted always-on item class' insight (B insight 3 / rec 3; C insight 2 / rec 3) is the strongest transferable finding in either document, but as written it reads as a duplicate of something Pack E already lists: recommendation.md:156 already enumerates 'MCP manifests, plugin metadata'. The distinction must be stated explicitly or the reviewer will discard it -- Pack E today counts the manifest FILE on disk; what pi shows is that the tool DESCRIPTION and GUIDELINE TEXT a connected server or plugin injects into the system prompt is a separate, larger, per-turn item. Verified consequence worth adding: that text survives compaction (the prompt is rebuilt from the loader at agent-session.ts:1061-1075 and the system block is retained per docs/compaction.md:72-78) while a loaded skill body does not (tool results are cut to 2000 chars, compaction/utils.ts:88-89). One caveat to correct on the way in: the rebuild iterates ACTIVE tool names (validToolNames at :1062), not every installed tool, so B's 'every package tool' and C's 'any package tool' both overstate slightly.
- SHARED (A, B, C) -- REAL. 'pi has the blocking gate Claude Code lacks' needs its containment caveat inline in the recommendation row, not only in section 7. Verified at pi:packages/coding-agent/src/core/extensions/runner.ts:982-1002: handlers run in extension load order, later handlers see earlier mutations, first block wins -- so handler ORDER, not privilege, decides, and a gate extension cannot contain an extension already loaded in-process. A insight 5 and B section 7 / rec 12 both state this correctly but in a different section from the recommendation that makes the claim. If the merged document proposes a pi gate, the honest claim is 'audits before load' and never 'contains', or F12/F13's no-safe-no-clean discipline is broken at the first use.
- MINOR, A is right and B and C are wrong. Event count: A's '36 on() overloads' is exact -- I counted 36 distinct event names in the ExtensionAPI on() overload block (types.ts:1252-1320). B and C both say '~40'. Use 36.
- C (the base) -- REAL gap the merge must close. C's decisions section asks about npm scope and ownership (P4) but never asks the prior question B's decision 2 raises: whether a TypeScript pi extension belongs in a Go repo at all, given recommendation.md:120 records that [SA:AGENTS.md:6] bans Python and the whole architecture is 'one Go binary, zero-dep' (recommendation.md:110). Shipping a published npm/TS artifact is a new artifact class and a lockstep-release maintenance commitment against a harness with an explicit no-backward-compatibility policy (pi:AGENTS.md:25). Carry B's framing of that decision into the base's section 8.

### Scorecard
## Coverage

18 cells walked (3 candidates x B1-B6); 17 claims spot-checked by me against the clones, the live npm/GitHub APIs, and `docs/research/` primaries (bar asked for 10); 3 structural audits per candidate (overlap-table CC/Cursor completeness, recommendation-table value+effort completeness, length proxy); 9 verifier summaries reconciled. Second sweep added nothing new.

## Verdict

**Rank: C > B > A. Base: C.** No candidate fails a must-pass. Ranking turns on shape (B4-B6).

## Scorecard

| Criterion | A (architecture-first) | B (overlap-first) | C (opportunity-first) |
|---|---|---|---|
| **B1** traceable, not refuted by source-accuracy | **Pass.** 15/0. Two cite drifts (`main.ts:111-122` lands on `resolveAppMode` — though the *claim* it supports is correct; `types.ts:1125-1134` is `ToolCallEventResult`, not `project_trust`). Most precise counts of the three (36 events, exact). | **Pass.** 15/0. Cites into `recommendation.md` are the most numerous and the most precise of any candidate (`rec:30,35,37,39,40,45,82,89,112,120,146-147,155-158,177,197-205,228,236` — I resolved ~15, all land). | **Pass.** 15/0. The one "not re-fetched" row I verified live and it is **right**: 104,234 stars, MIT, 1,981,039 weekly downloads, 9,654 npm packages keyword-tagged `pi-package` (sampled at `from=5000`, still all tagged). |
| **B2** overlap table + recs about skillgate for CC/Cursor | **Weakest.** Frame is architecture-first, so 15 rows of pi internals come before any overlap; its own relevance verifier found 7/15 sampled claims had no touch point. **6 of 34 CC/Cursor cells are em-dashes**, self-declared unverified. Offsetting strength: where filled, those cells cite SOTA/CTS/L2 primaries, not `rec:` — better provenance than B or C. | **Strongest anchoring.** Table is built column-by-column off our packs, plus a **G0→G7 gate-stage delta** naming what each stage must add — the most directly usable artifact in any candidate. (I reject the relevance verifier's 15/15 refutation: it applied a default-refuted stance — "pi isn't a target, so nothing overlaps" — that it did *not* apply to A or C, and that would void the mandate's own question.) Cells filled with `n/a`/`[I]` in 7 places. | **Pass, cleanest table.** All 17 rows filled across pi / skillgate / CC / Cursor with ●◐○ plus a note; no dashes. Alone in sizing *why* pi could matter to a CC+Cursor tool (adoption, verified). |
| **B3** CC value + Cursor value + effort, separately | 11/12 rows. Uses Low/Medium/High magnitudes — the most **differentiated** Cursor values. Row 12 (avoid): all dashes, no effort. | **10/12** — weakest. Rows 11-12 carry no value and no effort. Values are prose, not magnitudes. Credit for candour: row 7 Cursor = "none directly", row 10 = "none / none". | **12/12**, effort on every row including the avoid. But ~6 of 12 Cursor cells collapse to "Same" — coverage complete, differentiation thinner than A's. |
| **B4** insight quality (changes approach, stated reason) | One excellent idea — **pi as the Pack E ground-truth bench** (`systemPromptOptions` is exactly what enters the prompt, so our arithmetic is checkable against the real string; CC and Cursor cannot give us that). Also the "gate ordering, not privilege" candour rule → F12/F13. But raises the scope decision (D-A) with **no sizing at all**. | **Most operational.** The G0→G7 delta; "Pack E is under-scoped" as the sharpest single reframe; and the **invisible-skill** finding — verified REAL and unique to B: a `pi` manifest short-circuits convention dirs (`package-manager.ts:2174-2187`), so SkillSpector's own `skills/skill-inspector/SKILL.md` never loads. | **Most decision-shaped.** Its four questions *are* the four calls the user must make. Uniquely supplies the decision-grade number (104k / 2M weekly / 9.6k pi packages). Extends the tool-guidelines insight further than B: also a T001/T002 **injection** surface, and one that **survives compaction** while skill bodies are cut to 2,000 chars — verified. Widening the unit from *skill* to *package* (P3) transfers straight to CC plugin hooks and Cursor hooks. |
| **B5** candour about what pi does not do | **Strong.** 10-row §7 plus the best candour artifact in any candidate: a closing *"Unclear after this pass"* paragraph naming three specific unknowns, including its own unverified table cells. | **Good but holed.** §7 row admits *"not re-read under this lens"* — an `[I]` inside the candour table. No "what's still unclear" close. (I verified that row for the merge: spans are test-only, no OTel dep anywhere.) | **Best-shaped.** §7 is the only one with a **"Consequence for skillgate"** column, so candour is tied to our approach rather than generic. Concedes in §1 that pi dynamic hits "the same ceiling as Claude Code OTel", and that rec 1's CC/Cursor value is only "Indirect". |
| **B6** ≤5 pages, tables | ~82 rows, all tables, but the densest cells of the three. Mid-length. | **Longest** (~90 rows + extra prose block). | **Shortest** (~69 rows). Minor miss: §5 insights are prose, where the bar asks for tables. |

**All three overshoot five pages by roughly 2x** — a shared defect, not a discriminator.

## Why C is the base

C's organising frame is the four decisions the user actually has to make (borrow / third target / ship as a pi package / dynamic attribution), and every section answers them with a filled overlap table, 12/12 recommendations carrying both harness values and effort, and candour tied to consequence. It is also the only candidate carrying the evidence that makes the scope question answerable rather than rhetorical — and that evidence checks out live. B's frame is equally sound and better anchored to our gate, but it is the longest and leaves 2 of 12 recommendations unscored. A's architecture-first frame is the wrong shape for a bar that penalises generic pi commentary; its real strengths (precision, primary-sourced harness columns, the best candour close) are execution quality inside a weaker frame.

## Merge sequence

1. Take C whole.
2. Fold B's G0→G7 gate-stage delta into §4 under the overlap table; shrink C's recs 2/5/6/11 to one line each against it.
3. Fold A's Pack E ground-truth-bench reasoning into §5 behind C's rec 9, and name it as a slice-2 acceptance test.
4. Apply the 13 defects — the cache-miss mis-borrow, the activation overclaim, the bash leg of the gate, and the "already live" overclaim are the four that change conclusions.
5. Cut §2 to the rows §4 cites and §3 to the thin-shell lesson plus B's invisible-skill finding; that is where the page budget comes from.

## Critic
The document is strong on pi itself: every load-bearing pi claim I spot-checked is true at 71dca87 (manifest-only packages skip convention dirs, package-manager.ts:2174-2186; ExecOptions lacks env, exec.ts:11-18; before_agent_start exposes systemPrompt + systemPromptOptions, types.ts:717-727; first {block:true} short-circuits, runner.ts:982-1003; git installs run npm install --omit=dev with no --ignore-scripts, package-manager.ts:1772-1777,1856; SkillSpector package.json is private:true with an extensions-only manifest; skillspector-pi still 404 on npm and pi latest is 0.85.1 MIT as of today). The SkillSpector extension reading (~140 lines, one registerTool, dropped env, passive, terminal default, details without findings) matches /scratchpad/skillspector-judge/extensions/skillspector.ts line for line. Where it falls short of the user's question: (1) the Cursor column of the overlap map is wrong and contradicts the project's own research — Cursor natively loads Agent Skills and ingests ~/.claude/skills, which changes the cross-harness-ingestion story and several rec values; (2) the flagship recommendation (publish skillgate-pi on npm shelling to a Go binary) leaves the binary-distribution question open, and the only obvious answer (postinstall download) is the lifecycle-script pattern the same document flags as a supply-chain finding; (3) the Pack E ground-truth claim is stated in the wrong units and omits tool input schemas, which are billed prompt tokens outside systemPrompt; (4) pi's own evals package, the SYSTEM.md/APPEND_SYSTEM.md/--system-prompt surfaces, the experimental plugin/server API, and auto-install-on-trust were not examined although the mandate lenses cover them; (5) licensing (pi MIT vs SkillSpector Apache-2.0 for the code being copied) is never stated. Nothing here invalidates the insights; several would change effort ratings and two would change map cells.

- [high] Overlap map Cursor column asserts 'no skills system' (row 'Skill roots + cross-harness ingestion', citing rec:100,102) and marks Cursor ◐ on activation. Cursor natively discovers SKILL.md from .cursor/skills, .agents/skills, ~/.cursor/skills, ~/.agents/skills and, for compatibility, .claude/skills, ~/.claude/skills, .codex/skills, ~/.codex/skills; the project's own cts:113 (cursor.skill.source enum includes workspace|claude|plugin) and open-unknowns.md:29-35 (U-02 names .cursor/skills/*/SKILL.md) already imply this. That makes Cursor the stock cross-harness ingester pi is only opt-in for, changes rec #2's Cursor value ('same for AGENTS.md' undersells it), and means Cursor also has the multi-root collision problem insight #5 treats as pi-specific. — Fetch https://cursor.com/docs/context/skills (I confirmed the directory list and three activation modes today) and re-score the Cursor cells in rows 'Skill roots', 'Spec conformance', 'Activation signal' with a URL citation; cross-reference docs/research/cursor-telemetry-surfaces.md:113 and open-unknowns.md:29-35,81-89; update rec #2 and #6 Cursor value columns accordingly and record the discrepancy in research-contradictions.md.
- [high] Recommendation #1 (publish skillgate-pi to npm, shell to the Go binary) never says how the Go binary reaches a pi user. pi's npm install leg runs `npm install <spec>` with peer resolution disabled but no --ignore-scripts (package-manager.ts:1785-1811, getNpmInstallArgs; git leg 1772-1777), so the natural postinstall-download pattern is exactly the lifecycle-script supply-chain finding G0/#5 proposes. The alternatives (per-platform optionalDependencies carrying the binary, or require skillgate on PATH and fail closed into checks_skipped[]) have different effort and different verdict semantics, and the effort rating 'M' assumes one of them silently. The document also characterizes lifecycle scripts only for git installs; the npm leg is unstated. — Read package-manager.ts:1785-1830 and docs/packages.md 'Dependencies' section in the pi scratchpad; enumerate the three distribution options with the pi loader's aliasing constraint (host pi APIs resolve through loader aliases, peers are not installed) and pick one in P4/P5; state explicitly whether skillgate-pi's own install is a G0 finding under its own rules.
- [high] Insight #2 / rec #9 claim pi proves 'Pack E's arithmetic against ground truth' and line 8 says attribution is 'exact'. Two things are missing: (a) the units — before_agent_start gives a string, so ground truth is characters, and the only token number is first-turn usage.input+cacheRead+cacheWrite, which also contains the user message and every tool's input JSON schema; (b) tool input schemas (e.g. skillspector_scan's typebox schema, skillspector.ts:8-25) are always-on billed tokens that sit outside both systemPrompt and BuildSystemPromptOptions and are typically larger than the promptSnippet/promptGuidelines insight #1 counts. They are reachable via session.agent.state.tools (docs/sdk.md:250) or the RegisteredTool map (types.ts:1771), not via the event the document relies on. — Read docs/sdk.md:240-262 and types.ts:1765-1775 in the pi scratchpad; add 'tool input schema' as a Pack E item class beside injected guideline text; restate §5 #2 as 'exact in characters for the prompt string; tokens via first-turn usage minus user message, with tool schemas enumerated from state.tools'; adjust rec #3 and #9 accordingly.
- [high] packages/evals was not examined although the mandate's 'packages overview' lens covers it. It is a purpose-built harness that adapts a real AgentSession to vitest-evals, runs in isolated temp project/agent dirs, compares baseline vs candidate configurations of prompts/tools/skills/models with pass-rate, tokens, latency, est. cost, supports --repetitions, and attaches native session JSONL (packages/evals/README.md:1-80). That is rec #7's paired A/B comparator and the rec:38 precision/recall three-arm run, already built; the document instead proposes driving `-p --mode json --no-skills --skill` by hand (effort M). — Read packages/evals/README.md in full and packages/evals/src/*.eval.ts (evalHarnessTable, report.json, runs.jsonl schemas) in the pi scratchpad; decide whether rec #7 becomes 'borrow: run skillgate's three-arm design as a pi eval suite' and re-rate effort; check whether runs.jsonl records the skill set per arm so activation precision/recall can be scored from artifacts.
- [medium] Always-on surfaces omitted from the 'Always-on prose files' map row and the G5 delta: .pi/SYSTEM.md and ~/.pi/agent/SYSTEM.md replace the default prompt (skills and context files still appended), APPEND_SYSTEM.md in either location appends, and --system-prompt/--append-system-prompt do the same per run (docs/usage.md:115-118,243-244; resource-loader.ts:1024-1043; trust-manager.ts:36-37 shows SYSTEM.md is trust-gated while CLAUDE.md is not). Pack E's denominator on pi changes when SYSTEM.md is present, and a package cannot ship one but a repo can, which matters for G2/T020. — Read resource-loader.ts:1015-1050 and docs/usage.md:110-120 in the pi scratchpad; add SYSTEM.md/APPEND_SYSTEM.md (trust-gated) and the two CLI flags to the map row and to the G5 and G2 deltas; note the customPrompt path in system-prompt.ts:49-60 still appends context files and skills.
- [medium] Rec #9's 'first job' (Pack E acceptance test) is scoped as a live before_agent_start session (effort M, needs a provider key). The SDK exposes DefaultResourceLoader, loadProjectContextFiles, loadSkills, formatSkillsForPrompt (src/index.ts:202,272-276) and BuildSystemPromptOptions (index.ts:67), and createAgentSession yields session.agent.state.systemPrompt before any model call (docs/sdk.md:249). An offline, keyless test can rebuild the exact prompt string in CI; the live route is only needed for the token leg. — grep src/index.ts in the pi scratchpad for `buildSystemPrompt` (the function, not just the type) and try createAgentSession with a resourceLoader and no provider; if the string is obtainable offline, split rec #9 into S (offline string bench) + M (live token leg) and move the S part into slice 2 as P1 already intends.
- [medium] API-stability risk (P4, #11, §7 last row) treats the extension API as stable-with-lockstep, but 0.85.1's changelog (CHANGELOG.md:37) shows an `experimental/plugin` subpath, `client`, and server/client commands built on 'durable Session and Agent Harness interfaces' (packages/server, packages/protocol, packages/chord 'application-composition runtime for systems assembled from plugins/extensions'). Whether extensions remain the long-term unit or are being superseded by plugins is unexamined and bears directly on the lockstep maintenance commitment P4 asks the user to accept. — Read packages/coding-agent/src/experimental/plugin.ts, src/experimental/plugins/{bundled,package}.ts, packages/server/README.md and packages/chord/README.md in the pi scratchpad; search github.com/earendil-works/pi issues/discussions for 'plugin' vs 'extension' roadmap; add one line to §7 and to P4 stating whether the extension API is the durable surface.
- [medium] Auto-install on trust is not in the threat delta: project `.pi/settings.json#packages` triggers installMissing on startup once the project is trusted (package-manager.ts:1271-1301; docs/packages.md:43) — i.e. a git clone plus npm install with lifecycle scripts, fired by a single 'trust' answer that an extension can also auto-answer via project_trust. G2's T020 delta lists writes into settings.json#packages but not this declarative one-click install. Separately, the G0 delta names only pi.extensions and pi.skills as artifact classes; pi.prompts (slash-command templates that instruct the model like a skill) and pi.themes are also shipped by the same manifest (pi-manifest.ts:4-9). — Read package-manager.ts:1260-1310 and docs/packages.md:40-50; add 'repo-declared packages installed on trust' as a G0/G2 rule (T020 analogue) and add prompts/themes to the package artifact enumeration in §4 G0 and insight #4.
- [medium] Overlap cell 'Claude Code ● strict per docs (sota:116)' on the Spec-conformance row is not supported by the cited line: sota:114-118 covers listing truncation and stickiness, not validation strictness. Insight #5 ('the same file is an error on CC and a warning-with-reason on pi') rests on this cell. — Cite the Claude Code skills documentation section on frontmatter validation (or a CC changelog line) for what CC rejects vs warns on; if none exists, mark the cell [I] and note that Pack D's per-harness matrix is still source-less for CC, consistent with rec:155.
- [medium] Licensing is never stated. pi is MIT (LICENSE; packages/coding-agent/package.json:98; npm registry license MIT), SkillSpector is Apache-2.0 (LICENSE; skillspector.md:143 already records that vendoring needs LICENSE+NOTICE+change notices). Rec #1 says 'copy' the ~140-line shell and rec #10 says copy pi's check-entry-graphs.mjs / test.sh patterns; the document does not distinguish reimplementing a pattern (no obligation) from lifting lines (NOTICE obligation for Apache-2.0 code, attribution for MIT). — Add a licensing line to §3 and rec #1/#10: pi MIT (attribution if lines copied), SkillSpector Apache-2.0 (NOTICE if lines copied), and state that skillgate-pi is written from scratch against the pattern; cross-reference skillspector.md:143.
- [medium] The gate's leak surface lists read and bash legs only. pi's grep tool accepts a `context` parameter that prints N lines before/after each match from the file (tools/grep.ts:29,135,202-203), so `grep '' <SKILL.md> context=500` dumps the body through a third built-in leg with its own GrepToolCallEvent (types.ts:919-922). Rec #4's claim text ('blocks the read path; the bash path is unsolved') should name grep as well, or the gate should cover it since its input is structured, not a shell string. — Read tools/grep.ts:20-60,190-210 in the pi scratchpad; either add a grep clause to the rec #4 gate (structured path + context>0 → same pin check) or extend the stated bound to 'read and grep blocked; bash unsolved'.
- [low] Additional SkillSpector extension defects relevant to the 'contract' rec #1 sets, not checked or listed: (a) truncateText cuts stdout at 12,000 chars (skillspector.ts:47-53,111), so `format: json`/`sarif` without `output` hands the model a truncated, unparseable document with only a 'stdout truncated: true' line — the structured-output path is broken by default; (b) isLikelyUrl (skillspector.ts:30) classifies any `a/b` relative path as a URL and skips cwd resolution, while SkillSpector's own _is_git_url only accepts https:// or git@ (input_handler.py:934-951) — harmless only because pi.exec runs in ctx.cwd; (c) PI_EXTENSION.md:53-58 instructs users to pass provider/model via the tool, which the dropped env makes a silent no-op — a doc-vs-code mismatch stronger than the document's 'fixture' framing; (d) SKILLSPECTOR_BIN env override (skillspector.ts:60) lets anyone controlling the environment substitute the binary; (e) PI_EXTENSION.md:46 omits the nv_inference provider the schema lists. — Add rows to §3 from skillspector.ts:30,47-53,60,111 and docs/PI_EXTENSION.md:46,53-58; for skillgate-pi specify: never truncate structured formats (write to a temp file and return the path in details), resolve paths only via isAbsolute/resolve, no env-selectable binary path without a pin check.
- [low] Minor factual: §2 says 36 distinct `on()` event names at types.ts:1257-1320; a grep over the whole types.ts for `on(event: "…"` yields 33 distinct names. Also SkillSpector's peerDependencies use `>=0.78.1`, not the `*` the document calls 'the documented convention' — worth stating because pi's npm install disables peer resolution entirely (package-manager.ts:1785-1811), so the `tsc`-against-pinned-host-types CI in rec #1 must pin host types as devDependencies, not peers. — Re-run `grep -o 'on(event: "[a-z_]*"' src/core/extensions/types.ts | sort -u | wc -l` in the pi scratchpad and correct the count; add the devDependency-pinning detail to rec #1's effort note citing package-manager.ts:1785-1811 and ss:package.json:10-14.
- [low] P6 (oh-my-pi fork) and the pi.dev/packages gallery submission path (docs/packages.md:137) are declared unexamined; the `pi -e npm:…` try-without-install mode (docs/packages.md:46-50) is not mentioned though it is the cheapest way for a reviewer to run skillgate-pi without touching settings — relevant to the quarantine-first stance in G0. — Read docs/packages.md:44-50,137-150; add `pi -e` as the recommended zero-persistence invocation in rec #1; leave P6 as-is unless the user asks.

