# pi integration — delta log against `recommendation.md`

**2026-09-12 · integration pass only. No new research beyond re-verifying four load-bearing cites. No code.**

`pi.md` (arena-merged, pi @`71dca87`) was folded into `recommendation.md` **only where it changed a decision, a threat row, a gate stage, a refusal, a roadmap slice or a don't-build item**. The recommendation was not rewritten: **15 in-line edits**, each tagged **[pi]** with a cite to a `pi.md` section. 249 → 253 lines (4 new table rows, 11 in-line clauses). Lines below are post-edit.

| # | Where (§ · line) | Before | After — the **[pi]** fold-in | `pi.md` |
|---|---|---|---|---|
| 1 | Keys · L5 | seven keyed inputs | registers `[pi.md §n]`, points here | — |
| 2 | §2 threat row 7 · L31 | hook execution surface, CC + Cursor paths | **the audited unit is the package, not the skill**: one manifest ships `skills` *and* `extensions` — in-process TS, never type-checked, full privileges **and resolved provider auth** | §5 #4, §8 P3 |
| 3 | §3 **G0** · L55 | quarantine a fetched skill, hash it | provenance inputs are **package specs** (`npm:@ver` / `git:@ref` / local); missing ref ⇒ **unpinned**; git leg runs with **no `--ignore-scripts`** ⇒ lifecycle script is a supply-chain finding; `pi.extensions` are executables ⇒ ×1.3 | §4 G0, §5 #4 |
| 4 | §3 **G1** · L56 | terminal outcome per file, allow-listed enum | enum gains **`silently skipped`** — harnesses drop skills with *no error*; a silent drop is a ledger row, never an absence; collisions carry `{winnerPath, loserPath}` | §4 G1, §5 #5 |
| 5 | §3 **G7** · L62 | pin the hash on APPROVE | pin also records **the harness version the integration was tested against**; pi's peer convention is `*` with no back-compat guarantee, so the range is ours to assert | §4 G7, §6 #11 |
| 6 | §3 Cursor · **new row** L99 | Cursor = `.mdc` + `AGENTS.md`; skills unmentioned, read as absence | **correction: Cursor ships a stock skills system** — 4 native + 4 compatibility roots (`.claude/`, `.codex/`), recursive, nested repo roots auto-scoped, `name` must match the folder. ⇒ `~/.claude/skills` is live in Cursor with zero config, so portability findings hit Cursor **first**; agnix `CUR-*` cover `.mdc` **not `SKILL.md`**, so that conformance is ours; **root precedence is undocumented** ⇒ multi-root collision is a Cursor finding class needing a fixture | §4a, §5 #5 |
| 7 | §3 Cursor pre-activation · L100 | `beforeReadFile` deny on an unapproved path | **claim scoped to the read path**; the `bash` leg is open on every harness examined, so "no un-pinned `SKILL.md` enters context by any tool" is **not achievable today** — bash leg into `checks_skipped[]` | §7 row 2, §6 #4 |
| 8 | §4 **T017** · L147 | hooks in `.claude/settings.json` / `hooks/hooks.json` | **extends** to a `package.json#pi` manifest naming bundled TS — explicitly an extension, **not a 21st rule**, so the cap at 20 and §7's don't-build both hold | §4 G2, §5 #4 |
| 9 | §4 **T020** · L150 | writes into `.claude`, `.cursor`, a shell rc | adds `.pi/`, `~/.pi/agent/trust.json`, `settings.json#packages` — the trust file is pi's only input-loading guard, so writing it auto-answers the prompt | §4 G2 |
| 10 | §4 **Pack E** · L157 | counts MCP manifests + plugin metadata (the file on disk) | two missing classes, both bigger: **(a)** text an installed tool **injects each turn** (active tools; **survives compaction** when a skill body does not); **(b)** the **tool input schema**, billed every turn via the `tools` parameter, never in the prompt string — measured 1,007 vs 255 chars, **3.9×** | §5 #1, #2a, §6 #3 |
| 11 | §5 refusals · **new F14** L193 | F12, F13 | **a runtime gate audits *before* load; it never contains what already loaded, and never claims the `bash` leg** — load order decides, later handlers see earlier mutations, any extension may re-register `read`/`bash`. Candour extended to **CC hooks and Cursor `beforeReadFile`**. F13 bounds the ledger; F14 bounds the gate | §7 rows 1-2, §8 P2 |
| 12 | §6 **slice 1** · L201 | ships `skill-gate` in both plugin manifests | if D9 admits a third packaging target, the manifest **must list `skills` beside `extensions`** — a manifest short-circuits the directory walk, so an extensions-only manifest ships the binary and **silently drops the skill**, i.e. the interface (SkillSpector ships this defect live). **Acceptance test: the skill is discoverable after install, not only the tool** | §3 row 1, §6 #1 |
| 13 | §6 **PR 2** test · L214 | fixture estate → per-item table | **named slice-2 acceptance test, the ground-truth bench**: check the per-item arithmetic against the **assembled system-prompt string** pi hands an extension at `before_agent_start` — **exact in characters**; CC and Cursor expose no such string. **Tokens are a separate claim**, reconciled by subtraction and `count_tokens` within its stated tolerance | §5 #2, §6 #9, §8 P1 |
| 14 | §7 don't-build · **new row** L230 | — | **never use a cache-miss detector as the per-skill cost signal**: it measures prefix *invalidation*, a `SKILL.md` **appends**, so it reads ≈0 on every skill, has no attribution, and emits dollars against "never prices anything". **Invariant:** the number derives only from a quantity that changes when *and only when* a skill body enters context — the prompt-token delta, an **upper bound** | §5 #7, §6 #12 |
| 15 | §8 · **new D9** L247 | D1-D8 | **is pi a third target or only a bench fixture?** Names the new artifact class against `[SA:AGENTS.md:6]` and "one Go binary, zero-dep". Default: **static yes** (slices 2-3); **package yes, thin shell only** — logic in Go, binary from `SKILLGATE_BIN`/PATH, absent ⇒ `checks_skipped[]` + CAUTION ceiling, **never a `postinstall` download** (the pattern G0 flags; silently no-ops on 2 of 3 package managers); **pi dynamic behind slice 4** except the slice-2 bench | §8 P1, P4, P5 |

## Deliberately not integrated

- **§4 G4** (`.pi/settings.json` shape is our delta) — pi.md marks the enumeration leg `[I]`, and it binds only if D9 says target; scope detail inside a settled shell-out.
- **§4 G1's literal diagnostic codes**; **§6 #2** (pi roots in `skillgate env`), **#6** (Pack D pi column), **#10** (entry-budget scripts, `env -i` isolation, ICM index) — build backlog downstream of D9, not decisions. #6's *Cursor* leg **was** taken, as edit 6.
- **§4 G3 · G6** — pi.md states them unchanged.
- **§6 #7/#8, §5 #6** (pi session reader, dynamic mining) — deferred behind slice 4 by P1's own default. **Carry-forward:** if pi dynamic is ever admitted, pi.md's caution that pi activation is *reconstructed* and must never become activation ground truth (it would corrupt the slice-4 precision/recall comparator) must land as a refusal then. #8's cost half is already covered by edit 14.
- **§6 #12's other avoid-items** (pi telemetry/spans, upstreaming into pi core, regex example gates as security) — nothing in the recommendation proposes them; pi emits no telemetry at all.
- **§7 rows** (`allowed-tools` unenforced, no sandbox, no tokenizer) — qualify pi-side findings only; the recommendation never asserts enforcement on a third harness.
- **§8 P6** (oh-my-pi fork) — unexamined; pi.md's default is "no until asked". **§8 P7** — concerns pi.md's own length.
- **§5 #2's full token subtraction and §6's install-flag forensics** — cited by result in edits 13 and 15; the detail stays in pi.md rather than growing a fourth appendix.

**Open action this pass could not take.** `pi.md §4a` also directs that the Cursor stock-skills correction be filed as **contradiction #6 in `research-contradictions.md`**. That file was outside this task's two-file allowance and is **unedited**; someone must file it.

## Target, fixture, or neither

**As of 2026-09-12 pi is a fixture, not a target** — nothing in the roadmap ships pi support until the user answers **D9**, and pi's only committed role is the slice-2 ground-truth bench, a thing the tool is measured *against*; D9's default, if the user says nothing, promotes it to a static + packaging target across slices 1-3 with pi dynamic still behind slice 4.

---

**Provenance.** Integration pass over `docs/research/pi.md` (arena-merged, pi @`71dca871bc80b6bc97be37f0ca3189399d651fff`) into `docs/research/recommendation.md`; both read in full, under the closing-sequence rule in `.scuba/teams/recommendation/mandate.md`. Four load-bearing cites were re-verified against primary source before the edits resting on them: Cursor's eight skill roots, recursive walk, nested repo roots and folder-name rule (`cursor.com/docs/skills.md`, fetched 2026-09-12 — lines 61-78, 105-127, 172); pi's manifest short-circuit, `return true` after the manifest branch and *before* the convention-directory walk (`packages/coding-agent/src/core/package-manager.ts:2174-2189` at the pinned commit); `BeforeAgentStartEvent.systemPrompt: string` + `systemPromptOptions` (`core/extensions/types.ts:716-727`); and pi's skills doc instructing `bash` when `read` is unavailable, beside the `tool_call` note that later handlers see earlier mutations with no re-validation (`docs/skills.md:68`, `types.ts:935-944`). Every other claim traces to a `pi.md` section cited in-line; nothing was added that `pi.md` does not carry. Edits applied by exact-match replacement with a one-match assertion each; table shape re-checked per edited row. Date: **2026-09-12**.
