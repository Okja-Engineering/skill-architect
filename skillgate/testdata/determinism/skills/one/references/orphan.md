# Orphan

Nothing in this bundle references this file, so SK-G003 reports it. It is
here so the repeated-run probe covers the reachability emitter too: the walk
seeds from a map-free sorted entry list and the finding order follows the
ledger, and this fixture is what proves that stays true.
