# S2 — Hook install/uninstall + real fixture harvest + activation spike

- **Stage:** parked
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S1
- **Blocks:** (soft) S3's end-to-end assertion

## Goal
`hooks install` / `hooks uninstall` merge and unmerge `~/.cursor/hooks.json` without touching foreign hooks; one real Cursor session harvests redacted fixtures; and the spike answers R-RL-17.

## Definition of done
A fake `$HOME` seeded with a foreign hook survives install *and* uninstall byte-identical apart from our entries; a timestamped backup exists; the spike reports yes/no on whether `beforeReadFile` fires for a `SKILL.md` path.

## Requirements
R-CS-21, R-CS-23, R-CS-25, R-SA-09, R-SA-10, R-RL-17

## Files
`tests/test_hooks_install.sh` (new), `profiler/cmd/skill-scope/` (`hooks`, `doctor`), `tests/fixtures/`

## Next
**Blocked on User Question 1** — Cursor is not installed on this machine (`~/.cursor/` absent), and no fixture can be harvested without the user installing the hook on a machine that runs Cursor. The install/uninstall half is buildable and testable against a fake `$HOME` today; the harvest and the spike are not. Consider splitting if question 1 stays unanswered. Merge the hooks.json JSON with `jq`, never `python3`.
