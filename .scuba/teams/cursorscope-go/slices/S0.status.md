# S0 — Fix the Cursor OTel adapter against the wire reference

- **Stage:** ready
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** none
- **Blocks:** S6, S7

## Goal
Stop the shipped Cursor adapter lying. Correct every OTel attribute key to its Wire-Reference form, rewire `tool_calls` off `cursor.hook.execution_complete` onto the `cursor.tool.calls` metric, and retract the false SQLite token capability.

## Definition of done
A fixture written in the real wire-reference attribute shape yields `tokens`, `tool_calls`, and `skill_activation` `present` with correct values; `Probe()` on a bare `state.vscdb` reports `tokens: none` with a reason, not `sqlite`.

## Requirements
R-RL-01, R-RL-02, R-RL-03, R-RL-05, R-SA-14, R-SA-02

## Files
`profiler/cursor.go`, `profiler/types.go` (additive `ActivationEntry.Source`), `profiler/profiler_test.go`

## Next
Dispatchable to a bug-fixer **now**, independently of the rest of E-CS. This is a defect in merged code — it does not wait on roadmap approval, on Cursor being installed, or on any user answer. Cut `epic/skill-scope` from `main` first, or land it directly as a standalone fix PR to `main` if the manager prefers.
