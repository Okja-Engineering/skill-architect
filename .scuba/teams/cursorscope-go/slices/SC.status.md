# SC — SPLIT at round 5. See `SC-a.status.md` and `SC-b.status.md`.

`SC` no longer exists as a slice. Round 5 cut it on the seam `roadmap.md` §H2 named (V5-#15, both round-5 hunters concurring): **SC-a** = `types.go` + `tokens_select.go` + `validate.go` + spec doc (channels, selector, validator, caveat carrier); **SC-b** = `compare.go` + the comparison/experiment goldens (comparators, the gate, the single-`successes` rule, the `context_window` comparator). Item numbering is preserved across the two files, so a citation to "SC item 8" resolves to SC-a item 8 and "SC item 14" to SC-b item 14. Dependencies: S1b, S3, S6, S8a, S8b, S9, S10 → **SC-a**; S0, S5, S7 → **SC-a + SC-b**; SC-b → SC-a. Disposition: `review/revision-log-round5.md` V5-15.
