# S9 — Admin API token join (CONDITIONAL)

- **Stage:** parked — conditional
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S1, S5 · **gated on User Question 2** (Cursor plan tier)
- **Blocks:** none

## Goal
Join hook `conversation_id` to `POST /teams/filtered-usage-events` → `conversationId` and get genuinely billed per-request tokens — the cheapest real-token path for Cursor that does **not** require Enterprise OTel.

## Definition of done
With a Team admin key, a hook-sourced profile's `tokens` become `present` with `Source: "server_api"` and four-way splits (`inputTokens`, `outputTokens`, `cacheWriteTokens`, `cacheReadTokens`); without a key, the metric is `unknown` with an actionable reason and nothing else changes.

## Requirements
R-CS-26 (flipped from drop), R-RL-07, R-RL-08, R-SA-02, R-SA-13

## Files
`profiler/cursor_admin_api.go` (new), `profiler/cursor.go`, `profiler/profiler_test.go`

## Next
**Do not dispatch until the user confirms a Team or Enterprise plan and an admin API key.**

Do **not** port cursorscope's poller (`src/cursor-api-poller.js`): it uses `GET /teams/{teamId}/daily-usage-data` with Bearer auth. The documented endpoint is `POST /teams/daily-usage-data` with Basic auth (API key as username) and no team path segment — and `daily-usage-data` has **no token fields at all**. Target `filtered-usage-events` instead. Constraints: aggregated hourly, poll at most once per hour, 20 req/min per team, 30-day max date range.
