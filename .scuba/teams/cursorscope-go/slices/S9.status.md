# S9 — Admin API token join (CONDITIONAL)

- **Stage:** parked — conditional
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** **SC-a** (the `server_api` honesty class, `TokenCandidate` and the accumulator DoD 3 registers into; declared rather than left transitive, V4-#16), **S1b** (the hook-sourced profile it joins onto, and the `enrich()` call site it registers into), **S5** (its `estimated` producer is what DoD 3's mixed-candidate assertion needs) · **gated on User Question 2** (Cursor plan tier)
- **Blocks:** none

## Goal
Join hook `conversation_id` to `POST /teams/filtered-usage-events` → `conversationId` and get genuinely billed per-request tokens — the cheapest real-token path for Cursor that does **not** require Enterprise OTel.

## Definition of done
1. With a Team admin key, a hook-sourced profile's `tokens` become `present` with `Source: "server_api"` and the four-way splits mapped onto `TokenCounts` explicitly — `inputTokens→Input`, `outputTokens→Output`, `cacheReadTokens→CacheRead`, **`cacheWriteTokens→CacheCreation`** (the wire field is named "write", the contract field "creation"; the mapping is stated so it cannot drift). Field names are **Specification**, ledger §A5.3:180. The join key is supplied on both sides: `filtered-usage-events.conversationId` (§A5.3:180, optional on the wire) and the hooks' base `conversation_id` (§A5.1:113).
2. Without a key, the metric is `unknown` with an actionable reason and **nothing else in the profile changes** (R-SA-13).
3. **This slice registers a candidate and calls nothing that assigns (D12, V3-#2, V4-#1, V5-#3).** It `Add`s `TokenCandidate{Source: "server_api", SessionScoped: true}` to the `*TokenCandidates` accumulator threaded through `enrich()` — the join runs inside the enrichment seam, so **S1b's single `ApplyTokenSelection` call site (SC-a item 8) applies it**, and this slice adds no call site and no assignment. SC-a's rank puts `server_api` above every other label. Round 2 had this slice writing `tokens` directly while S5 also did, so whichever ran last silently overwrote the other's label — the failure D12 exists to prevent. Every field of `TokenCounts` in a `server_api` result comes from the Admin API response; no field is retained from a discarded `estimated`, `mcp_reported`, `otel` or `otel_aggregate` candidate.
4. A response row whose `conversationId` is absent (it is an **optional** field, §A5.3:180) contributes nothing and is not silently attributed to the profile's session.

## Test approach
`profiler/admin_api_test.go` (new, this slice's own file): an `httptest` fake Admin API returning a canned `filtered-usage-events` body authored from the docs (§G6 — no key, no call and no real response body has ever been observed, so the fixture is Specification, not capture). Assert: the join matches on `conversationId`; a response containing other conversations contributes nothing; a row with **no** `conversationId` contributes nothing (DoD 4); a missing key degrades to `unknown` and never fails hard (R-SA-13); the result carries `Source: "server_api"` and therefore maps to the `measured_billed` honesty class — **which is all this slice asserts about comparison. The cross-class refusal itself (`estimated` baseline vs `server_api` candidate) is proven once, in SC-b's own test; re-asserting it here would give this slice an SC-b dependency for a fact it does not own** (V5-#15); a spool that also yields an `estimated` candidate produces `Source: "server_api"` with **every** `TokenCounts` field sourced from the API body (DoD 3); and an AST/grep assertion that this file and `cursor_admin_api.go` **assign `Profile.Tokens` nowhere and add no second `ApplyTokenSelection` call site** (guardrail 5(a)'s property plus the round-5 positive half, V5-#3 — the negative half alone is satisfied by an inert file). `cursor_admin_api.go` is in S8a's Cursor-path file set, so guardrail 5 covers it.

## Requirements
R-CS-26 (flipped from drop), R-RL-07, R-SA-02, R-SA-13 · **D12**
**Not claimed:** R-RL-08 is tagged **drop** — it is a do-not-port instruction, carried below as a note rather than as a requirement this slice satisfies.

## Files
`profiler/cursor_admin_api.go` (new), `profiler/cursor.go`, `profiler/admin_api_test.go` (new)
No `cursor.*` OTel literal appears here — the Admin API's JSON keys (`conversationId`, `tokenUsage`) are not in the `cursor.` namespace, so S8a's guardrail 4 and D5 do not bind this slice.

## Next
**Do not dispatch until the user confirms a Team or Enterprise plan and an admin API key.**

Do **not** port cursorscope's poller: it uses `GET /teams/{teamId}/daily-usage-data` with Bearer auth (`src/cursor-api-poller.js:44,47` — **External evidence**, that file lives in the cursorscope clone at `cursor-profiler/tmp/research/cursorscope/`, so it is a third-party artifact per `AGENTS.md:20`, not a Repository fact). The documented endpoint is `POST /teams/daily-usage-data` with Basic auth (API key as username) and no team path segment — and `daily-usage-data` has **no token fields at all** (**Specification**, ledger §A5.3:179,186). Target `filtered-usage-events`.

**Rate limit and date range — one reading, not two (C4-#5).** Round 3 left this paragraph self-contradicting: it withdrew round 2's invented endpoint attribution, then reported the reviewer's live-doc figures without adopting them, so the slice named two incompatible pictures and adopted neither. The figures this slice builds against, adopted:
- **60 requests/minute** for `POST /teams/filtered-usage-events`;
- **the 30-day maximum date range does apply to this endpoint**, so a longer window must be chunked;
- data is aggregated hourly, so poll **at most once per hour** (ledger §A5.3:182).
Label: **Specification, from the live Admin API docs re-read at the round-4 gate (2026-09-10)** — not from the ledger, which cannot settle it: §A5.3:177 states *"Rate limits 20–250 req/min by endpoint. 30-day max date range."* as properties of **the API as a whole**, and :182 states *"20 req/min per team"* under `filtered-usage-events`. Where the ledger and the live docs disagree on a number, the live docs win and the ledger line is noted as stale. §G6 stands: no key, no call and no response body has ever been observed here, so **re-verify all three figures on the day this slice is built** — it is a one-page read and it changes the poller's backoff and its date-window chunking.
