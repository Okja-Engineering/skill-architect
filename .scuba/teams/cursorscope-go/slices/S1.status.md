# S1 — Hook JSON → spool → Profile (the proof)

- **Stage:** ready
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** none
- **Blocks:** S2, S3, S4, S5, S7, S9, S10, S11

## Goal
A Go binary reads one Cursor hook payload on stdin, appends a normalized event to a JSONL spool, and `profiler capture --harness cursor --hook-spool <f>` emits a schema-valid `Profile`. First consumer of `SourceHooks`.

## Definition of done
`tool_calls` and `timing` are `present` with `Source: "hooks"` from a hand-authored spool; the profile round-trips byte-identically; the hook process exits 0 in under 50 ms, measured.

## Requirements
R-CS-01, R-CS-02, R-CS-03, R-CS-05, R-CS-06, R-CS-07, R-CS-15, R-CS-28, R-CS-30, R-SA-01, R-SA-05, R-SA-08, R-SA-13

## Files
`profiler/hooks.go` (new), `profiler/cmd/cursor-hook/main.go` (new), `profiler/cursor.go`, `profiler/cmd/main.go`, `profiler/profiler_test.go`

## Next
Dispatchable in parallel with S0 under a different owner — the two touch `profiler/cursor.go` in disjoint functions. Note: hook field names are **Specification** from Cursor's docs, not captured payloads; the golden fixture is hand-authored and S2 replaces it with a real one.
