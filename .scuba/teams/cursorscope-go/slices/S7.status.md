# S7 — Cursor adapter unification + F04 end-to-end

- **Stage:** parked
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S0, S1, S3, S4, S5
- **Blocks:** none — this is the reconciliation point

## Goal
Hooks become a first-class source alongside the OTel export file, and `experiment plan` / `experiment run` can drive a hook-sourced capture, closing the loop back to F04.

## Definition of done
`Probe()` reports hooks and OTel independently; `Condition` / `ExperimentStep` carry a `HookSpool` path; `profiler compare` on two hook-sourced profiles returns `comparable: true` for `tool_calls` **and** `skill_activation`.

## Requirements
R-SA-06, R-SA-13, R-SA-15, R-RL-16

## Files
`profiler/cursor.go`, `profiler/experiment.go`, `profiler/cmd/main.go`, `profiler/profiler_test.go`

## Next
Everything upstream must be merged first. Expect one merge conflict in `profiler/cursor.go` where S0's and S1's lines meet — plan for it rather than serializing the whole epic to avoid it.

Carry R-RL-16 forward as a note to F04, not as work in this slice: the comparison report should express Skill Lift with a confidence interval, not a point estimate (ACES: mean 0.2134, 95% CI [0.1967, 0.2301] over 947 paired cases; positive-lift rate only 72.8%, so the engine must be able to report "this skill hurt"). A single paired run is not evidence.
