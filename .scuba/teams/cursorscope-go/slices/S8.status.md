# S8 — Docs, scope amendment, guardrails

- **Stage:** parked
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S3, S4
- **Blocks:** none

## Goal
Say what the plugin now actually does, with evidence labels, and make the four footguns fail CI.

## Definition of done
`docs/cursor-hooks-spec.md` exists with an evidence label on every claim; `.out-of-scope.md` and `README.md` reflect live capture; CI fails on a seeded violation of each of the four guardrail rules and passes once removed.

## Requirements
R-SA-11, R-SA-17, R-RL-15

## Guardrail rules (grep + go vet + Go tests — not semgrep)
1. A `python3` invocation in any shell script (`AGENTS.md`).
2. A `MetricResult` built as `present` from a function whose name contains `estimate` (R-SA-03 / R-RL-06).
3. An unredacted prompt or payload written to disk (R-CS-18/19).
4. A hardcoded `cursor.*` telemetry string outside the one file allowed to declare them (R-SA-14).

## Files
`docs/cursor-hooks-spec.md` (new), `.out-of-scope.md`, `README.md`, `.github/workflows/ci.yml`, `tests/test_guardrails.sh` (new)

## Next
**Partly gated on User Question 8** — `.out-of-scope.md` and `README.md` currently promise the plugin does *not* run live comparisons; amending them is the user's call.

Ledger Q3 settled the semgrep question: it cannot count tokens (`skill-validator` does, exactly, with `o200k_base`), parses markdown only in `generic` mode (≈`grep`), and is a Python package that `AGENTS.md`'s self-containment rule excludes. Do not add it.
