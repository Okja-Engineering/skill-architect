# S10 — OTLP export (CONDITIONAL)

- **Stage:** parked — conditional
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S1, S5 · **gated on User Question 5** (Fork ii)
- **Blocks:** none

## Goal
`skill-scope export --spool <f>` POSTs OTLP/HTTP JSON traces + metrics to a configured endpoint — hand-rolled with `encoding/json` + `net/http`, zero dependencies.

## Definition of done
A fake collector receives correctly nested spans (`conversation_id` → `generation_id` → `tool_use_id`) with wire-correct attribute keys; `profiler/go.mod` still declares **zero** requires; no `chars/4` estimate appears in any `gen_ai.usage.*_tokens` field.

## Requirements
R-CS-16, R-CS-17, R-CS-24, R-CS-29, R-SA-12, R-RL-13, R-RL-14

## Files
`profiler/otlp.go` (new), `profiler/cmd/skill-scope/`, `profiler/profiler_test.go`

## Next
**Do not dispatch until the user confirms they actually want an OTLP backend** (Last9, Grafana, Honeycomb, a local collector) rather than a local JSON spool.

Fork (v) is closed by measurement, not preference: the OTel Go SDK is 2 direct + 22 indirect modules, 408 packages compiled (including 66 gRPC packages despite being HTTP-only), 17.7 MB; the stdlib OTLP/JSON equivalent is 0 modules and 6.4 MB. If a future requirement genuinely forces the SDK, isolate it behind an interface in a **separate module** at `profiler/exporters/otlp/` so the core `profiler` module stays dependency-free.

Namespace our own skill attributes as `skill_architect.skill.*`. OTel semconv has **no** skill or rule convention, and `gen_ai.*` is Development status at semconv 1.44.0 — map to it only where the convention actually defines the field. Model the fake collector on cursorscope's `test/fake-collector.mjs` as a Go `httptest.Server`.
