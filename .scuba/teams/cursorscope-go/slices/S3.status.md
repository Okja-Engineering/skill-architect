# S3 — Skill attribution + activation from hooks

- **Stage:** parked
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S1 (S2's fixtures strengthen the end-to-end assertion but do not block the table tests)
- **Blocks:** S5, S7, S8

## Goal
Port `classifyInvocation` / `skillNameFromPath` into Go and produce the first non-`unknown` skill activation any adapter in this repo has ever emitted. This is the epic's prize.

## Definition of done
A `Profile` reports `skill_activation: present` and `attribution: present` naming the exercised skill, with `Trigger` explicitly marked as *inferred from a file read* — not a Cursor activation event.

## Requirements
R-CS-08, R-CS-09, R-CS-10, R-CS-29, R-SA-07, R-SA-02, R-SA-11

## Files
`profiler/attribution.go` (new), `profiler/hooks.go`, `profiler/profiler_test.go`

## Next
Rests on ledger §A5.1 finding 3: **there is no skill activation hook in Cursor**. This slice produces an inference and must label it as one. Its whole mechanism is contingent on R-RL-17 (S2's spike) — if Cursor loads skills without firing `beforeReadFile`, this slice yields nothing and the epic's value case collapses to S0 + T1. Transcribe cursorscope's `attribution.test.js` / `gen-ai-semconv.test.js` cases as the behavioral oracle.
