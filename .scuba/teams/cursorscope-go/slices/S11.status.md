# S11 — Daemon + HTTP ingest (CONDITIONAL)

- **Stage:** parked — conditional
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S1 · **gated on Fork (i) being reopened**
- **Blocks:** none

## Goal
A long-running `skill-scope serve` accepts `POST /cursor/hooks` as an optional second sink onto the same spool the stdin path writes.

## Definition of done
POSTing the S2 fixtures through the server produces a spool byte-equivalent to the stdin path; a backdated entry is closed and marked stale by the TTL sweep rather than leaked.

## Requirements
R-CS-04, R-CS-06 (original in-process form), R-CS-22, R-CS-23

## Files
`profiler/cmd/skill-scope/serve.go` (new), `profiler/hooks.go`

## Next
**Lowest priority in the epic; recommend it never ships.** Fork (i)'s default deletes this requirement rather than porting it. The daemon is the sole reason cursorscope needs a pidfile, auto-start bash (~104 lines in `ensure-cursorscope.sh`), version-mismatch restart, and a TTL sweep — and it shipped a memory-leak fix in 0.3.7 for exactly the long-lived state this reintroduces.

It only becomes worth building if the user needs real-time steering or multi-machine fan-in. The design is deliberately reversible: the daemon consumes the same spool the stdin path writes, so nothing upstream changes if it lands later. Test the TTL sweep with backdated timestamps, following cursorscope's `_testHooks` pattern.
