# S5 — Token honesty: three sources, three labels

- **Stage:** parked
- **Owner:** unassigned
- **Branch:** none (target: `epic/skill-scope`)
- **Depends on:** S1, S3
- **Blocks:** S7, S9, S10

## Goal
Add `SourceEstimated` to `MetricSource` and represent the three token sources — `chars/4`, MCP self-reported, and `preCompact.context_tokens` — so no reader can mistake any of them for billed tokens.

## Definition of done
No estimate ever serializes with `Source: "otel"` or `"hooks"`; each of the three sources has a fixture proving its label; `docs/profiler-spec.md` documents the representation and states the structural undercount.

## Requirements
R-CS-11, R-CS-12, R-CS-13, R-SA-03, R-RL-06, R-RL-10, R-RL-18

## Files
`profiler/types.go`, `profiler/hooks.go`, `docs/profiler-spec.md`, `profiler/profiler_test.go`

## Next
**Blocked on User Question 3** (Fork vi). Default is option (2) — `SourceEstimated` with `state: present`. If the user's honesty bar rejects any estimate in a comparison report, revert to option (1): tokens stay `unknown` and the `chars/4` figure lives in a separate, clearly-named field. The two options produce materially different DoDs; do not start until answered.

Record in the spec doc: the `chars/4` estimate structurally undercounts billed input (billing is the cumulative conversation re-sent each turn; the estimate is per-event payload bytes) and misses the system prompt, the always-loaded skill listing, and the cache-read/cache-creation split entirely. `preCompact.context_tokens` fires **only on compaction**.
