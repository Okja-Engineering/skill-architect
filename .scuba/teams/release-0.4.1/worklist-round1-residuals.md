# Round-1 residuals — to close in the item-19 (OTLP parser) pass

_Chief of staff, 2026-09-13 16:35. From the confirming pass on 527ba4e. Code half of round 1 is CLEAN at the root (W1–W16, W18 proven, tests RED without the fix). These ride with the parser build because it rewrites the same area and the branch has one writer._

- **R-F2 (docs truth, REAL).** `CHANGELOG.md` "verified live against a clean install" and `RELEASE_NOTES.md` "verified live" claim the GitHub marketplace route, which cannot work until `.claude-plugin/marketplace.json` is on `main`. Only the local route was exercised. Invariant: a "verified" claim names what was exercised; the remote route says "works once this release is on main".
- **R-F3 (contract layering, REAL).** `CaptureOpts.ExportFile` (types.go) is still silently ignored by the adapter; only the CLI refuses the flag. A library caller gets the "not configured" misdirection one layer down. Invariant: the adapter, which owns the contract, refuses or reads a supplied input; the CLI guard is not the boundary. Decide: delete `ExportFile` from `CaptureOpts` for the claude_code adapter (it is documented as ATIF/session-transcript input for other adapters that do not exist at this base), or make `Capture` return the same loud error the CLI does. Prefer the smaller honest option and state it in the spec.
- **R-F4 (dead code, REAL).** `profiler/cmd/main.go` `ExportFile: *exportFile` is provably always empty after the guard. Remove.
- **R-F5 (docs precision, SUSPECTED).** "42 cases counting subtests" double-counts two container functions; distinct cases are 40. Restate as functions and subtests separately, per commit, the way the changelog already does for earlier counts.
- **R-F6 (spec wording, SUSPECTED).** AC10 "never `present` with a zero value" is contradicted by a legitimately read zero. Reword: never present without a value read from the export; a read zero is a value.
- **R-F7 (reason string, SUSPECTED).** Attribution reason now says "does not read Claude Code's skill attribution attributes", presupposing they exist; research says no output-to-skill mapping exists. Item 19's skill_activation/attribution wording decides this family once: activation `unknown` because user-skill names are redacted without OTEL_LOG_TOOL_DETAILS; attribution `unknown` because no output-to-skill mapping exists in the telemetry.
- **R-obs.** `captureFlagError` keys on a hardcoded adapter fact rather than the adapter; fine today, note in a comment. `capture` exits 0 with `error`-state signals on an unreadable export; document that "loud" means in the JSON, or exit non-zero, and pin whichever.
- **W17 bookkeeping: CLOSED.** The confirming hunter read status.md before the fixer's 16:12 write; at head the file carries "Per-W disposition" (line 223), "Found, not fixed — round 1" (387), and "ROOT A / mandate item 19 — deliberately untouched" (418) with the pending-OTLP list. F1 is INVALID against the current file.
